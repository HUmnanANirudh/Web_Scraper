{
  "name": "amazon-scraper",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"npx puppeteer install && node index.js",
    "build": "echo 'No build step required'",
    "postinstall": "puppeteer install"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.19.2",
    "puppeteer": "^23.2.1"
  },"engines": {
    "node": "20.x"
  }
}
